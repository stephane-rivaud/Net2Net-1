[project]
name = "net2net"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
  "matplotlib",
  "numpy",
  "pytest",
  "torch",
  "torchvision",
  "wandb>=0.29.0",
]

[tool.uv]
package = false
